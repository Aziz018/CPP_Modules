#pragma once

#include <algorithm>
#include <iostream>
#include <sstream>
#include <vector>
// #include <map>

void fordJohnson(std::vector<int>& c);
void parseInput(int ac, char* av[], std::vector<int>& container);
